export class departmentdtO {
    id?: number; // L'identifiant du département (optionnel)
    name!: string; // Le nom du département (obligatoire)
    managerId?: number; // L'identifiant du responsable du département (optionnel)
  }
  